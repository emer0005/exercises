const fullName = "emilie";

const firstletter = fullName.substring(0, 1);
console.log(firstletter);

const lastPart = fullName.substring(1);
console.log(lastPart);

console.log(firstletter.toUpperCase() + lastPart.toLowerCase());
